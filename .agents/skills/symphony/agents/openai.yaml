interface:
  display_name: "Symphony"
  short_description: "Repo-specific patterns and workflows for symphony"
  default_prompt: "Use the symphony repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true